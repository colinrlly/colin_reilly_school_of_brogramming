calculator flask app

setup:
$ python -m venv venv
$ source venv/bin/activate
$ pip install -r requirements.txt
$ cp .env.dist .env
$ flask run

/api/calculate : POST
body:
  {
      "to_calculate": "2 + 1"
  }

response:
  {
      "result": 3
  }
